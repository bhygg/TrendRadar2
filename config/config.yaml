# ================= 专业产业内参监测系统 =================
# 版本: 2.0 | 设计: 深度产业穿透 + 商业价值导向

app:
  version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version"
  show_version_update: false
  data_retention_days: 30
  log_level: "INFO"

crawler:
  request_interval: 1500
  enable_crawler: true
  use_proxy: false
  default_proxy: "http://127.0.0.1:10086"
  user_agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
  timeout: 30
  retry_times: 3

# 🎯 重要：测试时使用 daily 模式
report:
  mode: "daily"  # 测试时使用 daily 或 current，不要用 incremental
  rank_threshold: 3
  sort_by_position_first: true
  max_news_per_keyword: 5
  min_industry_depth: 2
  
  # 日报生成配置
  daily_report:
    generate_time: "08:00"
    include_trend_analysis: true
    format: "markdown"

# 📊 权重配置
weight:
  rank_weight: 0.35
  frequency_weight: 0.45
  hotness_weight: 0.20
  industry_boost: 1.5
  entertainment_discount: 0.7

# 🚀 重要：测试时关闭推送时间窗口
notification:
  enable_notification: true  # 确保通知开启
  message_batch_size: 5000
  feishu_batch_size: 30000
  batch_send_interval: 3
  feishu_message_separator: "━━━━━━━━━━━━━━━━━━━"
  
  # 🔴 测试时关闭推送时间窗口
  push_window:
    enabled: false  # 测试时设置为 false
    time_range:
      start: "08:00"
      end: "09:00"
    once_per_day: true
    push_record_retention_days: 7
  
  webhooks:
    feishu_url: ""  # 通过GitHub Secrets设置
    # 其他webhook留空

# 📈 平台配置（精简版，只启用关键平台）
platforms:
  # 财经投资类
  - id: "wallstreetcn-hot"
    name: "华尔街见闻 最热"
    weight: 1.5
    enabled: true
    
  - id: "cls-hot"
    name: "财联社热门"
    weight: 1.5
    enabled: true
    
  - id: "jin10"
    name: "金十数据"
    weight: 1.4
    enabled: true
    
  # 综合新闻类
  - id: "thepaper"
    name: "澎湃新闻"
    weight: 1.2
    enabled: true
    
  - id: "cankaoxiaoxi"
    name: "参考消息"
    weight: 1.2
    enabled: true
    
  # 科技类
  - id: "ithome"
    name: "IT之家"
    weight: 1.3
    enabled: true
  
  # 社交类（严格控制）
  - id: "weibo"
    name: "微博"
    weight: 0.8
    enabled: true
    
  - id: "zhihu"
    name: "知乎"
    weight: 1.0
    enabled: true

# 📊 存储配置
storage:
  data_dir: "./data"
  archive_dir: "./data/archive"
  reports_dir: "./reports"
  
  cache:
    enabled: true
    ttl_hours: 24
    max_size_mb: 100

# 🔧 功能配置
features:
  industry_chain:
    enabled: true
    max_depth: 4
    
  sentiment_analysis:
    enabled: false  # 测试时关闭复杂功能
    
  deduplication:
    enabled: true
    method: "simhash"
    threshold: 0.85

# 🎯 性能配置
performance:
  max_concurrent_requests: 3
  request_delay_ms: 2000
  timeout_ms: 30000
  max_retries: 2
