# ================= 舆论热点监测系统 =================
# 版本: 4.0 | 设计: 重要舆论热点 + 市场情绪监测
# 特点: 专注重大事件、政策变化、社会热点、市场情绪

app:
  version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version"
  show_version_update: false
  data_retention_days: 30
  log_level: "INFO"

crawler:
  request_interval: 1000  # 请求间隔(毫秒)
  enable_crawler: true
  use_proxy: false
  default_proxy: "http://127.0.0.1:10086"
  user_agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
  timeout: 30
  retry_times: 3

# 🔸 推送模式选择
report:
  mode: "incremental"  # 默认增量模式，避免重复推送
  rank_threshold: 5  # 排名高亮阈值
  sort_by_position_first: true  # 按配置位置排序，确保重要关键词优先
  max_news_per_keyword: 5  # 每个关键词最大显示数量
  enable_hot_topic_clustering: true

# 📊 权重配置（追实时热点型）
weight:
  rank_weight: 0.7  # 主要看排名
  frequency_weight: 0.2  # 适度关注持续性
  hotness_weight: 0.1  # 降低热度权重
  
  hot_topic_boost:
    enabled: true
    min_platform_count: 3
    boost_factor: 1.3

# 🚨 通知配置
notification:
  enable_notification: true
  message_batch_size: 4000
  feishu_batch_size: 20000
  batch_send_interval: 2
  feishu_message_separator: "━━━━━━━━━━━━━━━━━━━"
  
  # 飞书消息格式优化
  feishu_message_format: "markdown"
  use_card_message: true  # 使用卡片消息，排版更好
  
  emergency_notification:
    enabled: true
    keywords: ["突发", "紧急", "重大", "爆炸", "地震", "事故", "冲突", "战争"]
    min_rank: 3  # 排名前三时紧急通知
    
  push_window:
    enabled: false
    time_range:
      start: "08:00"
      end: "22:00"
    once_per_day: false
  
  webhooks:
    feishu_url: ""  # 通过GitHub Secrets设置

# 📈 平台配置（优化后的核心平台）
platforms:
  # 核心舆论平台
  - id: "weibo"
    name: "微博"
    weight: 1.8
    enabled: true
    
  - id: "baidu"
    name: "百度热搜"
    weight: 1.7
    enabled: true
    
  - id: "zhihu"
    name: "知乎"
    weight: 1.6
    enabled: true
    
  - id: "toutiao"
    name: "今日头条"
    weight: 1.5
    enabled: true
    
  - id: "douyin"
    name: "抖音"
    weight: 1.4
    enabled: true
    
  - id: "bilibili-hot-search"
    name: "bilibili 热搜"
    weight: 1.3
    enabled: true

  # 新闻资讯平台
  - id: "thepaper"
    name: "澎湃新闻"
    weight: 1.2
    enabled: true
    
  - id: "cankaoxiaoxi"
    name: "参考消息"
    weight: 1.2
    enabled: true
    
  - id: "ifeng"
    name: "凤凰网"
    weight: 1.1
    enabled: true
    
  - id: "tencent-hot"
    name: "腾讯新闻 综合早报"
    weight: 1.1
    enabled: true

  # 财经资讯
  - id: "wallstreetcn-hot"
    name: "华尔街见闻 最热"
    weight: 1.0
    enabled: true
    
  - id: "cls-hot"
    name: "财联社热门"
    weight: 1.0
    enabled: true
    
  - id: "jin10"
    name: "金十数据"
    weight: 0.9
    enabled: true

  # 其他
  - id: "ithome"
    name: "IT之家"
    weight: 0.8
    enabled: true

# 📊 热点分析配置
hotspot:
  clustering:
    enabled: true
    method: "topic_modeling"
    similarity_threshold: 0.7
    
  sentiment_analysis:
    enabled: true
    method: "keyword_based"
    
  cross_platform_analysis:
    enabled: true
    min_cross_count: 2

# 🎯 性能配置
performance:
  max_concurrent_requests: 8  # 增加并发数提高效率
  request_delay_ms: 800
  timeout_ms: 20000
  max_retries: 3
  cache_hot_topics: true
  cache_ttl_minutes: 30

# 📁 存储配置
storage:
  data_dir: "./data"
  hot_topics_dir: "./data/hot_topics"
  reports_dir: "./reports"
  daily_summary_dir: "./reports/daily"
  
  archive:
    enabled: true
    keep_days: 30
    compress_old: true
